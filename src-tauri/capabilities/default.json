{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Main Smara Desktop window permissions for Rust-native IPC, event streaming, and local dialogs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default",
    "clipboard-manager:allow-read-image"
  ]
}
